apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: profile-db-persistent-volume-claim
spec:
  volumeName: profile-db-persistent-volume
  storageClassName: standard
  resources:
    requests:
      storage: 1Gi
  accessModes:
      - ReadWriteOnce